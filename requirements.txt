# ═══════════════════════════════════════════════════════════
# Weaver | نَسَّاج — Python Dependencies
# آخر تحديث: مارس 2026
# التثبيت: pip install -r requirements.txt
# ═══════════════════════════════════════════════════════════

# ── بوت تيليجرام ─────────────────────────────────────────
# v20.8: آخر إصدار مستقر في سلسلة 20.x
# ⚠️ لا ترقّه لـ v21 — يحتوي تغييرات جذرية تكسر الكود الحالي
python-telegram-bot==20.8

# ── طلبات HTTP (مهم للأمان) ──────────────────────────────
# v2.32.3: يُصلح CVE-2024-35195 (ثغرة أمنية في 2.31.x)
requests==2.32.3

# ── مكتبة Google Gemini AI ───────────────────────────────
# v0.8.4: يدعم Gemini 2.0 Flash (v0.3.0 لا يدعمه!)
google-generativeai==0.8.4

# ── معالجة الصور ─────────────────────────────────────────
# v11.1.0: يُصلح CVE-2024-28219 (buffer overflow في 10.3.x)
Pillow==11.1.0

# ── HTTP غير متزامن (مستخدم داخلياً بواسطة python-telegram-bot) ──
# v3.11.12: يُصلح memory leak في v3.9.x
aiohttp==3.11.12

# ── متغيرات البيئة (.env) ────────────────────────────────
# v1.0.1: إصلاحات بسيطة عن v1.0.0
python-dotenv==1.0.1
requests>=2.32.3
Pillow>=11.1.0
python-telegram-bot==20.8
